coverage:
  status:
    project:
      default:
        enabled: yes
        target: auto
        threshold: 1%
    patch:
      default:
        enabled: yes
        target: "55%"
        threshold: 2%
