coverage:
  status:
    project:
      default:
        enabled: yes
        target: auto
        threshold: 1%
    patch:
      default:
        enabled: no
        target: auto
        threshold: 1%
